const Locator = {
  dateSelect: '#datePanel > select',
  facilitySelect: '#facilityPanel > select',
  timeSelect: '#sessionTimePanel > select',
  areaSelect: '#areaPanel > select',
};

export default Locator;
